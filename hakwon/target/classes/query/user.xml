<?xml version="1.0" encoding="euc-kr"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="u">

	<!-- 학생 목록(학급 기준) -->
	<select id="get_user_c" parameterType="String" resultType="uBean">
		SELECT *
		FROM user
	</select>
	
	<update id="reg_user" parameterType="String" >
		INSERT INTO /* student.reg_user */
			user
		(
			  name
			, password
			, email
			, select_value
			, reg_dttm
			, reg_id
			, mdfy_dttm
			, mdfy_id
		)
	
		VALUES(
			  #{name}
			, #{password}
			, #{email}
			, #{select_value}
			, date('now')
			, 'admin'
			, date('now')
			, 'admin'
		)
		
	</update>
	
	<!-- 시도 목록 가져오기 -->
	<select id="get_sido" parameterType="String" resultType="java.util.Map">
		SELECT *
		FROM sido
	</select>
	

</mapper>